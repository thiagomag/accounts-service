server:
  port: 8081

spring:
  application:
    name: accounts-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:192.168.0.76}:${DB_PORT:5432}/${DB_NAME:accounts_service}${DB_URL_PARAM:?ssl=false}
    username: ${DATABASE_USERNAME:thiago}
    password: ${DATABASE_PASSWORD:Pedro@2007Aline10!}
    hikari:
      maximum-pool-size: 10

jwt:
  issuer: ${JWT_ISSUER:thiago-magdalena}
  secret: ${JWT_SECRET:4Z^XrroxR@dWxqf$mTTKwW$!@#qGr4PT}

security:
  public-paths:
    - "/swagger-ui/**"
    - "/v3/api-docs/**"
    - "/webjars/**"
    - "/swagger-resources/**"
    - "/auth-service/**"